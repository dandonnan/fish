using System;

namespace CommuteGL
{
    // Auto-generated when creating the MonoGame project.
    public static class Program
    {
        [STAThread]
        static void Main()
        {
            using (var game = new MainGame())
                game.Run();
        }
    }
}
